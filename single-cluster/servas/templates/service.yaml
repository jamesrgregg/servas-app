apiVersion: v1
kind: Service
metadata:
  name: {{ include "servas.fullname" . }}
  labels:
    {{- include "servas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "servas.selectorLabels" . | nindent 4 }}
